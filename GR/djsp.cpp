#include <bits/stdc++.h>
#define n size
#define graph vector<vector<int>>

using namespace std;

void djsp(graph g) {
	vector<vector<bool>> visited();
	for (int i = 0; i < g.n(); i++) {
		for (int j = 0; j < g[0].n(); j++) {
		}
	}
}

int main() {
}

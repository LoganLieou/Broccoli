#include <bits/stdc++.h>

using namespace std;

int main() {
	// don't sync with stdio and just go in poggers
	ios::sync_with_stdio(0);
	cin.tie(0); cin.sync_with_stdio(0);

	int t; cin >> t;
	while (t--) {
		cout << "based" << endl;
	}
}
